sudo: required

services:
  - docker

jobs:
  include:
    - stage: "Tests"
      name: "Run Mocha tests"
      env:
        - USER=ctudball
      install:
        - docker build -t $USER/hello-express
      script:
        - docker run $USER/hello-express test